// This component is for the schedule view -- like calendar view with draggable components

export default function Page = () {
  return <div>This will contain rehearsal blocks. Might be broken up into weekly views, or just one month block for now.</div>
}
